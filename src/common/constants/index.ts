/** */
export const PROJECT_NAME = 'Nest Boilerplate 🚀';
export const JWT = 'JWT';

/** */
export const THROTTLE = {
    TTL: {
        DEFAULT: 60,
        MIN: 1,
        MAX: 3600,
    },
    LIMIT: {
        DEFAULT: 10,
        MIN: 1,
        MAX: 100,
    }
}